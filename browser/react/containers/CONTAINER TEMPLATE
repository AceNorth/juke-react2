import {connect} from 'react-redux';
import Component from '../components/component';
import { methods } from '../utils'
import { action-creators for dispatch } from '../action-creators/wherevs';


function mapStateToProps(state, ownProps){
	let PROP1 = state.reducername.property;
	let PROP2 = state.reducername.whatevdata(MANIPULATED W/ UTIL METHOD)
	return {
		PROPS OBJECT
	}
}

function mapDispatchToProps(dispatch){

	return {
		ACTION-CREATORS REWRITTEN AS FUNCTIONS (so we don't have to use dispatch);
    	}
	}
}


export default connect(
	mapStateToProps, 
	mapDispatchToProps)(COMPONENT);